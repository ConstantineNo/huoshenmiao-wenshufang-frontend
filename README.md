# Cloud Print Web

远程前端客户端。

职责：

- 模板管理入口
- 业务录入与打印发起
- 目标节点与打印机选择
- 打印任务状态查看

当前阶段目标：

- 先建立最小 Vite + React + TypeScript 骨架
- 后续按 docs 仓中的云边端 CONTRACT 对接 REST API

开发启动建议：

```powershell
npm install
npm run dev
```
